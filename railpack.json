{
  "steps": {
    "extensions": {
      "commands": [
        "install-php-extensions gd exif ctype curl dom fileinfo filter hash mbstring openssl pcre pdo pdo_mysql session tokenizer xml"
      ]
    }
  }
}
